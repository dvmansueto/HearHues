<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- https://material.google.com/style/typography.html -->
    <style name="heading1" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/heading1_textSize</item>
    </style>
    <style name="heading2" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/heading2_textSize</item>
    </style>
    <style name="body1" parent="@android:style/TextAppearance">
        <item name="android:textSize">@dimen/normal_textSize</item>
    </style>

</resources>
